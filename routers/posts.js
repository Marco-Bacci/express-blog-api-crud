const express = require ('express');

const router = express.Router();


// index
router.get('/')
// show
router.get('/:id',)
// create
router.post('/',)
// update
router.put('/:id',)
// delete
router.delete('/:id',)

module.exports = router;